# Legacy Python API — not the active application

This earlier prototype is retained unchanged for reference. It is not used, started, or deployed by the functional MVP. Its mock authentication, trading-related endpoints, and in-memory storage do not represent the current product.

The maintained application lives at the repository root and uses Next.js Server Actions with Supabase Auth/Postgres. No Python service is required.
