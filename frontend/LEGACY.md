# Legacy prototype — not the active application

This directory is preserved as historical reference. Its old API fallback and authentication behavior are not part of the functional MVP and must not be used for client demonstrations or deployment.

Run the maintained Next.js application from the repository root. See the root README.md for Supabase setup and development commands. Do not run this frontend alongside the new app on the same port.
